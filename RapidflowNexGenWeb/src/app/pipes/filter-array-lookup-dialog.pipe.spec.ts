import { FilterArrayLookupDialogPipe } from './filter-array-lookup-dialog.pipe';

describe('FilterArrayLookupDialogPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayLookupDialogPipe();
    expect(pipe).toBeTruthy();
  });
});
